## [include] path = ~/.dotfiles/gitconfig
[core]
	filemode = false
	editor = vim
	ignorecase = false
	autocrlf = false
	excludesfile = ~/.dotfiles/gitignore
	whitespace = cr-at-eol
[alias]
	st = status
	br = branch
	ci = commit
	co = checkout
	pur = pull --rebase
	prune = fetch --prune
	abort = checkout -- .
	wait = reset HEAD
	undo = reset --soft HEAD^
	amend = commit --amend --no-edit
#
	l = log --pretty=oneline --abbrev-commit --graph --decorate
	changelog = "!sh -c \"git log --oneline --no-merges $1..HEAD\" -"
#
	df = !sh -c 'git diff --ignore-space-change --stat -p -w --color $* | $PAGER' -
	wdf = !sh -c 'git diff --word-diff --stat -p -w --color $* | $PAGER' -
	l  = !git log --graph --color | $PAGER
	diffarchive = !git diff master.. --name-only | xargs git archive --format=zip -v HEAD > diffarchive.zip
	export = !git archive --format=zip -v HEAD > archive.zip
	ghpage = subtree push --prefix dist origin gh-pages
	prune = pull --prune
[diff]
	compactionHeuristic = true
[color]
	ui = true
[user]
	name = Tosh Satake
	email = 2sm@csc.jp
[push]
	default = current
# vim: ft=gitconfig
